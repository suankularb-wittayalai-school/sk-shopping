// Imports
import { StylableFC } from "@/utils/types/common";

const ReceiptDialog: StylableFC<{
  open: boolean;
  onClose: () => void;
}> = ({ open, onClose, style, className }) => null;

export default ReceiptDialog;
